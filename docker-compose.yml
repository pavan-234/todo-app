services:

  backend:
    build: ./backend
    container_name: todo-backend
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
    dns:
      - 8.8.8.8

  frontend:
    build: ./frontend
    container_name: todo-frontend
    ports:
      - "5173:80"